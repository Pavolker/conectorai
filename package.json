{
  "name": "conectorai-backend",
  "version": "1.0.0",
  "description": "Backend para o Escriba de Capítulos",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
